doctype html
html
	head
		title Thunder
		
		meta(name="author" content="CloudKid")
		meta(name="description" content="{{description}}")
		meta(name="keywords" content="{{keywords}}")
		meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no")

		link(href="/css/libraries.css", rel="stylesheet")
		link(href="/css/main.css", rel="stylesheet")

		link(rel="stylesheet" href="http://fonts.googleapis.com/css?family=Open+Sans:300,400")
	body
		nav.navbar.navbar-inverse.navbar-fixed-top
			.container
				.navbar-header
					button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
						span.sr-only Toggle navigation
						span.icon-bar
						span.icon-bar
						span.icon-bar
					span.navbar-brand
						if (user)
							a(href="/admin")= user.name
							|  is bringing the 
							a(href='/') Thunder
						else
							a(href='/') Thunder
				div.collapse.navbar-collapse
					ul(class="nav navbar-nav navbar-right")
						if (user)
							li: a(href="/favorites" ).glyphicon.glyphicon-heart
							li: a(href="/add").glyphicon.glyphicon-plus 
							li: a(href="/admin" ).glyphicon.glyphicon-user
							li: a(href="/logout").glyphicon.glyphicon-log-out
						else 
							li: a(href="/login").glyphicon.glyphicon-user
		main.container
			.col-sm-9
				block content
			nav.col-sm-3
				include ../includes/search
				if categories
					include ../includes/categories
		.container.text-center
			footer &copy; 2015

		script(src="/js/libraries.js")
		script(src="/js/main.js")